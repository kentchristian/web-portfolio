export type overViewType = {
  icon: React.ReactNode | string;
  amount: number,
  trends: string;
  title: string;
}

export type projectsType = {
  image: string;
  title: string;
  description: string;
  techStack: string[];
}