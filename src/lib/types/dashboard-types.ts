export type overViewType = {
  key: string,
  icon: React.ReactNode | string;
  amount: number,
  trends: string;
  title: string;
}

export type projectsType = {
  key: string;
  image: string;
  title: string;
  description: string;
  techStack: string[];
}